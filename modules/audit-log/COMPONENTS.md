# Audit Log Module — Store Components

This module has no customer-facing store components.

Audit logs are an admin-only feature. All log entries are created server-side through the controller API and viewed through the admin interface at `/admin/audit-log`.
