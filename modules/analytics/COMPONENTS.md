# Analytics Module — Store Components

This module has no customer-facing store components.

Analytics tracking is handled server-side through the module's endpoint system. Event collection and dashboard visualization are admin-only features accessible at `/admin/analytics`.
