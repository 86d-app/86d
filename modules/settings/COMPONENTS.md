# Settings Module — Store Components

This module has no customer-facing store components.

Store settings and configuration are managed exclusively through the admin interface at `/admin/settings`. Store-facing behavior is influenced by settings values read server-side by other modules.
