# Square Module — Store Components

This module has no customer-facing store components.

Square is a payment provider module that implements the `PaymentProvider` interface from the payments module. It handles payment processing, webhook verification, and refunds server-side. Checkout UI is managed by the checkout module.
