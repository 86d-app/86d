# Import/Export Module — Store Components

This module has no customer-facing store components.

Import and export operations are admin-only features for bulk data management. All functionality is accessed through the admin interface at `/admin/import-export`.
